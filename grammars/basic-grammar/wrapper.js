// define a function
let f = (a, b, c) => {console.log(a, b, c)}

// define functions map
let f_map = {
    construct: (a, b) => console.log("constract with a and b ", a, b),
    call: c => console.log("call with c ", c),
    end: e => console.log("end with e ", e)
}

// 